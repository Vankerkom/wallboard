import { Routes } from '@angular/router';
import {MessagesPageComponent} from "./containers/messages-page/messages-page.component";

export const routes: Routes = [
  { path: '', component: MessagesPageComponent }
];
